# Claude Container

## Design Logs

Before implementing new features or significant changes, use the `design-log` skill to create a design log in `./design-log/`.

Review existing design logs before making changes to understand prior decisions and context.

When a feature adds or modifies environment variables, document them in the design log under an "Environment Variables" section.
